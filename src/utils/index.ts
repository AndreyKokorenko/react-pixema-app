import { getFirebaseMessage, FirebaseError, FirebaseErrorCode } from "./fireBaseError";
import { recommended } from "./recommended";

export { getFirebaseMessage, recommended, FirebaseError };
export type { FirebaseErrorCode };
